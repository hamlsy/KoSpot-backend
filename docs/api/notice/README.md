# Notice API Documentation

공지사항 관련 API 문서입니다.

## 📋 목차

- [공지사항 조회](#공지사항-조회)
- [공지사항 관리 (관리자)](#공지사항-관리-관리자)

---

## 공지사항 조회

### 1. 공지사항 전체 조회
**GET** `/notice/`

공지사항 전체 리스트를 페이징 조회합니다.

**Query Parameters**
| 필드 | 타입 | 필수 | 기본값 | 설명 |
|------|------|------|--------|------|
| page | Integer | X | 0 | 페이지 번호 (0부터 시작) |

**Response**
```json
{
  "isSuccess": true,
  "code": "2000",
  "message": "OK",
  "data": [
    {
      "noticeId": 1,
      "title": "서비스 점검 안내",
      "createdDate": "2025-01-01T10:00:00"
    },
    {
      "noticeId": 2,
      "title": "신규 기능 업데이트",
      "createdDate": "2025-01-02T10:00:00"
    }
  ]
}
```

---

### 2. 공지사항 단일 조회
**GET** `/notice/{id}`

공지사항 단일 게시물을 조회합니다.

**Path Parameters**
| 필드 | 타입 | 설명 |
|------|------|------|
| id | Long | 공지사항 ID |

**Response**
```json
{
  "isSuccess": true,
  "code": "2000",
  "message": "OK",
  "data": {
    "noticeId": 1,
    "title": "서비스 점검 안내",
    "content": "2025년 1월 5일 오전 2시부터 6시까지 서비스 점검이 진행됩니다...",
    "createdDate": "2025-01-01T10:00:00"
  }
}
```

---

## 공지사항 관리 (관리자)

> ⚠️ **주의**: 다음 API들은 관리자 권한(ROLE_ADMIN)이 필요합니다.

### 1. 공지사항 생성
**POST** `/notice/`

새로운 공지사항을 생성합니다.

**Headers**
```
Authorization: Bearer {access_token}
Content-Type: multipart/form-data
```

**Request Body (Form Data)**
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| title | String | O | 공지사항 제목 |
| content | String | O | 공지사항 내용 (HTML 지원) |
| images | File[] | X | 첨부 이미지 파일 (다중 업로드 가능) |

**Response**
```json
{
  "isSuccess": true,
  "code": "2000",
  "message": "OK"
}
```

**참고사항**
- `content`는 HTML 형식을 지원합니다.
- 이미지는 여러 개 첨부할 수 있습니다.

---

### 2. 공지사항 수정
**PUT** `/notice/{id}`

기존 공지사항을 수정합니다.

**Headers**
```
Authorization: Bearer {access_token}
Content-Type: multipart/form-data
```

**Path Parameters**
| 필드 | 타입 | 설명 |
|------|------|------|
| id | Long | 공지사항 ID |

**Request Body (Form Data)**
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| title | String | O | 공지사항 제목 |
| content | String | O | 공지사항 내용 |

**Response**
```json
{
  "isSuccess": true,
  "code": "2000",
  "message": "OK"
}
```

---

### 3. 공지사항 삭제
**DELETE** `/notice/{id}`

공지사항을 삭제합니다.

**Headers**
```
Authorization: Bearer {access_token}
```

**Path Parameters**
| 필드 | 타입 | 설명 |
|------|------|------|
| id | Long | 공지사항 ID |

**Response**
```json
{
  "isSuccess": true,
  "code": "2000",
  "message": "OK"
}
```

